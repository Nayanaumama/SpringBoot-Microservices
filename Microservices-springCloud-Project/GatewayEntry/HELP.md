
Here this is an API gateway using mvc-gateway and spring starter web .We can use webflux with spring cloud starter gateway for high latency realtime project . Then configuration syntax will be different . Here I am registering the gateway in to the 
eureka registry . ANd configuring in application.xml that every request from the user and hotels and rate can be called from the gateway itself
