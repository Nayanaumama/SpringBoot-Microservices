server:
  port: 8090
spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Nayanaumama/SpringBoot-Microservices
          clone-on-start: true